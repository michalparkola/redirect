{
  "name": "redirect",
  "version": "0.1.0",
  "description": "A simple URL shortening / redirect service.",
  "main": "redirect.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michał Parkoła",
  "dependencies": {
    "sqlite3": "^3.1.8"
  }
}
